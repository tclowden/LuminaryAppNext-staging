'use client'
import { ActionProps } from "./actionData"

const ActionTile = ({ options }: ActionProps) => {
   return (
      <>
         Conditional Paths
      </>
   )
}

export default ActionTile
