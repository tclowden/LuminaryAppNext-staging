'use client'
import { TriggerProps } from "./triggerData"

const TriggerTile = ({ options }: TriggerProps) => {
   return (
      <>
         Run automation whenever field is updated to
      </>
   )
}

export default TriggerTile
