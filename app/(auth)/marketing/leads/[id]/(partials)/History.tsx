import Panel from '../../../../../../common/components/panel/Panel';

type Props = {};

const History = ({}: Props) => {
   return (
      <Panel title='History' titleIconName='Clock' titleIconColor='gray' collapsible isCollapsed>
         History
      </Panel>
   );
};

export default History;
