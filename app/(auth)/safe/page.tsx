import React from 'react';

const Safe = () => {
   return (
      <div className='max-w-[600px]'>
         This page is here to safely redirect to, for now, if user doesn't have permissions or if there is no sidebar
         data...
      </div>
   );
};

export default Safe;
