'use client';

import React from 'react';

type Props = {
   user: any;
};
const DefaultDashboard = ({ user }: Props) => {
   return (
      <>
         <div>Default Dashboard</div>
      </>
   );
};

export default DefaultDashboard;
